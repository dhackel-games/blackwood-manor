{
  "name": "blackwood-manor",
  "version": "2026.9.11",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cucumber-js \"tests/features/**/*.feature\" --import \"tests/steps/**/*.js\"",
    "test:unit": "npm test -- --tags @unit",
    "test:walkthrough": "npm test -- --tags @walkthrough"
  },
  "devDependencies": {
    "@cucumber/cucumber": "12.9.0"
  }
}
